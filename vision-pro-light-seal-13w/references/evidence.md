# Corrected Light Seal interfaces

The current expanded scan-agreement verdict is **needs review**. `interface-validation-latest.json` is the canonical current report and the acceptance path declared by `measurements.toml`; `interface-validation.json` is a compatibility alias with the same current verdict. `vision-pro-narrow-lip-acceptance.json` also gates its `accepted` field on overall expanded acceptance. Failure, including an exception before measurement, updates all current discovery paths to a failing result. Older accepted reports are archived under `historical/` with top-level `accepted: false`, `status: historical_superseded`, and the original narrower outcome explicitly marked historical.

`python references/check_evidence.py` performs a read-only source, export, manifest, and runtime freshness check. A current failing report remains `current_failed` and exits nonzero; older accepted artifacts report `stale`. Supply an older report path to check its identity explicitly.

Run `python references/export_identity.py` before validation. It builds the current source once, publishes STEP/STL/3MF from that body, and publishes a manifest last. Standalone and full validation rebuild current source, compare its exact B-rep identity, check source/runtime identities and every export digest, and reject stale or equal-volume altered geometry. `python references/test_export_identity.py` exercises source drift, unbuildable source, changed exports, and equal-volume translation mutations. `python references/test_independent_scan_evidence.py` proves raw boundary extraction follows scan geometry and ignores authored slot dimensions. Regenerate native feature contracts and saved viewer setups with `python ../prepare_inspections.py` from the model project after evidence changes.

The narrow high-W rim connects to Vision Pro. Its requested feature is the small inverse-T retaining lip within 5.8 mm of the leading edge. The broad low-W side connects to the face cushion and has eight shallow closed attachment pockets. Earlier interface labels were reversed; broad-side profile statistics must not be reused as narrow-side accuracy claims.

`interface-validation.md` summarizes the current verdict. The canonical current JSON records identities for the validator, model source, card, measurements, all exports, export manifest, reference GLB, alignment, retained measurements, thresholds, and default parameters. Export binding records numpy, scipy, lib3mf, build123d, OCP, trimesh, nurb and Python versions plus the content hash of installed nurb Python and TOML sources, including editable installations. Raw manifest/export hashes are checked before and after rebuilding; interface validation watches them throughout inspection. Alignment must be finite, proper and rigid, and must exactly equal the card transform. Timestamp and UUID normalization is used for reproducible export identity, never to hide a concurrent raw-file replacement.

The independent geometry-only plane fit differs from the stored reference plane by 0.0043 degrees and 0.0060 mm. Stored-plane reflected-reference p95 is 0.778 mm against a 1.0 mm threshold. This verification uses deterministic area-weighted scan samples and excludes the documented cloth and nose mask without using texture.

The narrow-interface acceptance intersects the actual STEP with four retained local planes. Each available right and left scan path is evaluated independently over the complete 0 to 5.5 mm interval and the 2.2 to 4.2 mm inverse-T lip interval. Complete-profile bidirectional p95 must be at most 0.35 mm, lip bidirectional p95 must be at most 0.30 mm, projection must be 0.20 to 0.65 mm, and the rim must widen by at least 1.0 mm from neck to shoulder. `vision-pro-narrow-lip-sections.png` remains a visualization; its older side-pooled metrics are not acceptance evidence.

The cushion-opening acceptance samples the actual final STEP trim edges on both X sides and compares them bidirectionally with the scan-derived pair measurement. Contour p95 must be at most 0.05 mm and reflected pair p95 must be at most 0.025 mm. The separately observed scan-side center disagreement must stay inside each feature's recorded 0.4 or 0.5 mm dimensional uncertainty. The retained source measurement stores pair-averaged opening length and width plus separate side-center disagreement, not raw independent per-side opening polylines. The validator therefore does not claim per-side raw contour agreement.

`face-cushion-wide-seating-measurements.json` and `face-cushion-wide-return-guides.json` retain the correctly relabeled source measurements for the broad contact surface. `face-cushion-wide-pockets.json` records paired floor centers, support centers, outward normals, long axes, opening dimensions, depths, side-center disagreement, and uncertainty. Floor and support centers differ by the measured depth along the outward normal. Dark texture without matching depression is excluded. `measurements.toml` points acceptance to the overall `/accepted` verdict in the latest report; its separate retained-section pointer is diagnostic only. The older pooled metric and contour JSON files identify themselves as `superseded_visualization_only`; they cannot be treated as acceptance evidence.

Run `python references/validate_reconstruction.py` after preparing the default exports. It checks one connected body, export manifoldness, symmetry of the actual trimmed boundary including pocket edges, measured floor positions, small nonzero fit offsets, and then enforces current interface evidence. `validate_interfaces.py` can also refresh interface evidence directly. Both commands fail closed and exit nonzero when any requirement fails. Current reports always publish the current verdict; immutable historical files preserve earlier measurements. `test_validator_regressions.py` proves a displaced retained scan side updates current discovery to failed while leaving history untouched.

`alignment.json` preserves the original rigid mirror-plane fit. Original PLY, texture, textured GLB, and converter are unchanged. The original datum is corrected by one proper rigid matrix, with no scale, warp, or scan mirroring.

`face-cushion-wide-pocket-review.json` and `face-cushion-wide-pocket-thickness.json` verify actual exported floors and sampled remaining material. `face-cushion-wide-final-cad.png` shows all eight finished pockets. The 0.19 mm automatic thin-section finding is on the small narrow retaining lip, remote from these pocket floors.

Black nose-guard cloth, the light-seal interior, deep lining shoulders, magnets, ribs, cosmetic seams, and texture relief remain outside scope. Scan agreement is reconstruction evidence, not physical fit certification.
