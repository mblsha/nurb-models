# Current Light Seal interface evidence

Overall status: **failed**. Accepted: **false**. The canonical machine-readable result is `interface-validation-latest.json`; `interface-validation.json` is its compatibility alias.

Independent raw-scan measurements and overlays are described in `independent-scan-validation.md`. Their identity must match the current report before reuse. Historical limited acceptance lives only in `historical/` and does not establish current acceptance.

- new independent per-side or between-station evidence is outside the declared uncertainty or unresolved; inspect independent-scan-validation.json
